name: (DEPRECADO) Extração Playwright ANEEL

# ⚠️ DESATIVADO. O caminho oficial agora é atualizar.yml (CKAN direto).
# Este workflow usava Playwright no runner e era bloqueado pelo portal da ANEEL.
# Mantido só como referência histórica; roda apenas manualmente (sem agendamento).
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Aviso
        run: echo "Workflow deprecado. Use 'Atualizar Tarifas ANEEL (CKAN)' (atualizar.yml)."
